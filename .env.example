# REGULENS Environment Configuration

# Express Backend Port
PORT=5000

# Optional LLM API Key (System uses deterministic fallback engine if key is absent)
OPENAI_API_KEY=

# Node Environment
NODE_ENV=development
